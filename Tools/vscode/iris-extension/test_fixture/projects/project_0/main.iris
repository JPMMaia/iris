module project_0.main;

import c.complex as complex;

export function main() -> (result: Int32)
{
    var a: complex.Complex = explicit {
        real: 1.0f32,
        imaginary: 0.0f32
    };

    var b: complex.Complex = explicit {
        real: 0.0f32,
        imaginary: 1.0f32
    };

    var c = complex.add(a, b);

    return 0;
}
