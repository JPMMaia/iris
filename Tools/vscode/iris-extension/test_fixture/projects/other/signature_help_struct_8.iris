module signature_help_struct_8;

// Represents complex numbers. Uses 32-bit floats.
struct Complex
{
    // The real part.
    real: Float32 = 0.0f32;

    // The imaginary part.
    imaginary: Float32 = 0.0f32;
}

function run() -> ()
{
    mutable instance: Complex = {};
    instance = {
        real: 1.0f32,
    };
}
