module hover_struct_0;

// Represents complex numbers. Uses 32-bit floats.
struct Complex
{
    // The real part.
    real: Float32 = 0.0f32;

    // The imaginary part.
    imaginary: Float32 = 0.0f32;
}

function run(input: Complex) -> (result: Complex)
{
    var instance: Complex = {
        real: 1.0f32,
        imaginary: 1.0f32
    };
    var real = instance.real;
    var imaginary = instance.imaginary;
    return instance;
}
