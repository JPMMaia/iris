module Stack_array_loop;

export function foo(length: Uint64) -> ()
{
    var array_0: Array_slice::<mutable Int32> = create_stack_array_uninitialized::<Int32>(length);

    for index in 1u64 to length
    {
        var array_1: Array_slice::<mutable Int32> = create_stack_array_uninitialized::<Int32>(index);
        array_1[0] = index as Int32;
    }
}
