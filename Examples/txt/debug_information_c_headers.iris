module Debug_information;

import c.vector2i as vector2i;

export function run() -> (result: C_int)
{
    var a: vector2i.Vector2i = { x: 1ci, y: -1ci };
    var b: vector2i.Vector2i = { x: 2ci, y: -2ci };
    var c = vector2i.add(a, b);
    return c.x + c.y;
}
