module Pointers;

export function foo(data: *Byte) -> ()
{
}

export function pointers(external_pointer: *mutable Int32) -> ()
{
    var a = 1;
    var pointer_a = &a;
    var dereferenced_a = *pointer_a;

    mutable p0 = offset_pointer(external_pointer, 2i64);
    *p0 = 0;

    var data_array: Constant_array::<Uint32, 2> = [0u32, 1u32];
    var p1 = reinterpret_as::<*Byte>(&data_array[0]);
    foo(reinterpret_as::<*Byte>(&data_array[0]));
}
